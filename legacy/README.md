# Legacy — Deprecated v1

Folder ini berisi versi lama **XsaytUI.lua** (API v1 `CreateWindow`/`CreateTab`).

**Status:** `DEPRECATED` — dipindah kesini biar root rapi & gak duplikat dengan `XSAYT.lua` v2 full original.

- `XsaytUI.lua` — 976 lines, v1 lama, jangan pakai untuk project baru
- `example-legacy.lua` — contoh legacy

**Gunakan yang baru (v2 premium):**
```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/myzakonz-gif/UI-LIB-XSAYT/main/XSAYT.lua"))()
-- XSAYT.New({Title, Theme}) -> Hub:AddTab -> Section:AddToggle
```

File ini tetap ada biar link lama tidak 404, tapi tidak direkomendasikan.
